version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: arbitration_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"

  api:
    build: ./backend
    ports:
      - "8000:8000"
    depends_on:
      - db
